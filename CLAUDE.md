# CLAUDE.md

## 이 게임이 뭔가

**Clack & Crackle** (한국어 제목: **나만 없어 왁뿌볼**)

캐주얼 아트 인크리멘탈 ASMR 클리커.

화면 하단 중앙의 **키캡**을 누르면 골드가 나오고, 일정 타수마다 **왁뿌볼**이
필드로 나온다. 필드의 왁뿌볼을 **꾹 눌러** 부수면 골드와 환생재화를
얻는다. 골드로 **룰렛**을 돌려 키캡 조각·왁뿌볼·새 종류를 뽑고,
**환생**으로 스킬트리를 키운다.

레퍼런스: Steam "Click the Button"

## ★ 2인 작업이다 — 먼저 확인할 것

이 프로젝트는 두 사람이 레인을 나눠 병렬로 작업한다.
**`docs/11-WORKSTREAMS.md` 를 읽고 시작할 것.**

```
"뭐 하면 되나요?" / "다음 작업 뭐야?"
        → docs/11-WORKSTREAMS.md 의 "다음 작업" 표.
          자기 레인에서 맨 위 미완료 항목이 지금 할 일이다.

"이 파일 고쳐도 되나?"
        → 같은 문서의 "파일 소유권". 남의 레인 파일은 고치지 않고 요청한다.

레인을 모르겠으면 물어볼 것. 임의로 정하고 시작하지 말 것.
```

| 레인 | 영역 | 담당 문서 |
|---|---|---|
| **A · 엔진** | `src/core/` `scripts/` — 상태·경제·밸런스 | `02-ECONOMY` `07-ACHIEVEMENTS` |
| **B · 감각** | `render/` `audio/` `ui/` `assets/` — 손맛·화면·소리 | `03-ART` `04-AUDIO` |

두 레인의 유일한 접점은 **`src/core/state.ts` 의 `GameState` / `GameEvent`** 다.
여기를 건드리면 상대 레인이 즉시 영향을 받는다. 바꿨으면 바로 머지한다.

## 설계 논지

**소리가 보상이고, 숫자는 페이싱 장치다.**

판단이 갈릴 때 우선순위:
1. 부수는 순간의 손맛
2. 다음 부수기까지의 기다림이 지루하지 않은가
3. 숫자 밸런스

3번을 위해 1번을 희생하는 결정은 거의 항상 틀렸다.

## 이 게임은 인크리멘탈이다

이 문장이 대부분의 설계 판단을 결정한다. 구체적으로:

- **모든 성장 축은 상한이 없거나, 상한이 있으면 그 위에 무한 축이 있어야 한다.**
  엔딩이 없는 게임이므로 플레이어가 100시간째에도 올릴 게 남아야 한다.
- **손가락 속도가 게임의 상한이 되면 안 된다.** 그래서 자동화 3단계가 있다.
- **숫자는 커져야 한다.** `break_infinity.js` 를 쓰는 이유가 있다.
  20시간 시점에 골드가 백만 단위에 머물면 장르 기대치 미달이다.
- **환생재화는 진행도에 비례해야 한다.** 고정값이면 100회차가 1회차와 같아진다.

## 핵심 루프

```
키캡 클릭 ──▶ 골드(소량)
     │
     └──(N타마다)──▶ 왁뿌볼 ──▶ 필드(5칸)
                                   │
                        꾹 누르기 / 자동
                                   │
                     ┌─────────────┴──────────────┐
                 골드(주 수입)              환생재화(적립)
                     │                            │
                   룰렛                          환생
                     │                            │
    키캡조각 / 왁뿌볼 / 종류해금·레벨업 / 꽝    스킬트리
```

**키캡은 무료 공급원, 룰렛은 유료 업그레이드 경로.**
골드가 0이어도 키캡만 두들기면 게임이 돌아간다.

## 두 개의 축을 혼동하지 말 것

| 축 | 무엇 | 표기 | 변하는가 |
|---|---|---|---|
| **종류 희귀도** | 그 종류를 해금하기 어려운 정도 | ★1~★5 | 고정 |
| **개체 등급** | 이번에 나온 왁뿌볼의 등급 | 흰/초록/보라/금/프리즘 | 매번 랜덤 |

"★1 사과의 Supreme 개체" 같은 조합이 가능하다.
코드에서 `rarity`(종류 희귀도)와 `grade`(개체 등급)로 이름을 분리할 것.

## 절대 규칙

1. **`src/core/` 는 `pixi.js` 를 import 하지 않는다.** 순수 로직만.
2. **수치를 하드코딩하지 않는다.** 모든 밸런스 값은 `src/core/content/` 에만.
3. **큰 수는 `Decimal`(break_infinity.js).**
4. **에셋은 표시 크기의 2배 해상도로 제작한다** (레티나 대응). linear 필터로
   확대하므로 원본이 흐리면 그대로 흐려진다. (2026-09 캐주얼 전환 전에는
   "좌표·스케일은 정수만"이 이 자리였다 — 픽셀아트 전용 제약이라 폐기했다.
   `docs/06-DECISIONS.md` "아트 스타일 — 픽셀 → 캐주얼")
5. **왁뿌볼 종류와 등급은 직교한다.** 50×5 조합을 스프라이트로 굽지 말 것.
   종류 스프라이트 50 + 등급 틴트/이펙트 5 + 균열 오버레이 5 로 런타임 조합.
   ★ 캐주얼 아트는 그림에 음영이 baked-in 이라 그냥 곱하기 틴트를 하면
   부자연스럽다. 종류 그림을 **베이스 컬러 레이어 / 셰이딩 레이어**로
   나눠서 그려야 틴트가 자연스럽게 먹는다 (`docs/03-ART.md`).
6. **인앱결제를 도입하지 않는다.** 법적 제약이다.
7. 밸런스를 건드렸으면 `npm run sim`, 화면을 건드렸으면 `npm run shot`.
8. **이름은 i18n 키로만 쓴다.** `nameKey: "ball.type.001"` 이지, `name: "사과"` 가 아니다.
   고유명사가 74개이고 9개 언어로 나간다. 하드코딩하면 전부 뜯어내야 한다.
   **언어 추가는 JSON 1개 + `locales.ts` 1줄로 끝나야 한다** (`docs/10-I18N.md`).
   새 문자열은 `en.json` + `ko.json` 두 파일에만 추가한다. 나머지 7개는
   건드리지 않는다 — 폴백이 처리하고 `npm test` 가 남은 걸 세어준다.
9. **core 를 성능 이유로 mutable 하게 바꾸지 않는다.** 20시간 시뮬레이션이
   91ms 로 측정됐다. 순수 함수라는 성질이 성능보다 중요하다.
10. 커밋 전 `npm run check`.

## 구조

```
src/
  core/          순수 로직. pixi / howler 를 import 하지 않는다
    numbers.ts     Decimal, 포맷, 시드 RNG
    state.ts       GameState (3층 구조), GameEvent
    engine.ts      순수 함수 전부
    save.ts        직렬화 + 버전 마이그레이션
    content/
      balls.ts       종류 50 + 등급 5 (직교). 손으로 나열 말고 공식 생성
      keycaps.ts     24종
      roulette.ts    확률표, 비용 곡선
      skilltree.ts   노드 + 자동화 3단계
      audio.ts       사운드 정의 (데이터만)
  i18n/          ★ 문자열. 콘텐츠 데이터보다 먼저 존재해야 한다
    index.ts       t(key, params?) / setLocale. 라이브러리 안 씀
    locales.ts     LOCALES. 언어 추가 시 고치는 유일한 코드 파일
    en.json        ★ 키 집합의 기준. ko 외 8개는 여기로 폴백된다
    ko.json ...    T0/T1 9개 언어
  audio/         howler 재생. GameEvent 구독
  render/        Pixi. 상태를 읽고 그리기만
  ui/            HTML HUD
  platform/      web / steam 추상화
scripts/sim.ts   tsx 로 실행하는 밸런스 리포트
assets/          AssetPack 입력 → public/atlas/ 로 패킹
```

환경 구성 전체는 `docs/09-DEV-ENV.md`.

## 명령어

| | 언제 |
|---|---|
| `npm run dev` | 항상 켜두고 작업 |
| `npm run assets` | `assets/` 에 PNG 를 추가/수정한 뒤 |
| `npm run sim` | **밸런스 수치를 건드린 뒤 반드시** |
| `npm run shot` | **화면을 건드린 뒤 반드시** |
| `npm test` | 단위 테스트 |
| `npm run check` | 커밋 전 |

## 문서

| 파일 | 내용 |
|---|---|
| `docs/00-CONCEPT.md` | 컨셉, 루프, 리스크 |
| `docs/01-OPEN-QUESTIONS.md` | 미결. 임의로 정하지 말 것 |
| `docs/02-ECONOMY.md` | **전체 시스템 사양.** 작업 전 필독 |
| `docs/03-ART.md` | 캐주얼 아트 규격, 에셋 목록 |
| `docs/04-AUDIO.md` | 사운드 스펙 |
| `docs/05-SHIP.md` | 출시 체크리스트 |
| `docs/06-DECISIONS.md` | 결정 로그 |
| `docs/07-ACHIEVEMENTS.md` | 업적 |
| `docs/08-NAMING.md` | 이름 워크시트 (제목만 확정, 나머지 미정) |
| `docs/09-DEV-ENV.md` | 개발 환경, 명령어, 검증 방법 |
| `docs/10-I18N.md` | **현지화.** 제목, 지원 언어, 언어·문자열 추가 절차 |
| `docs/11-WORKSTREAMS.md` | **2인 분담.** 레인, 파일 소유권, 다음 작업. **작업 전 필독** |

## 하지 말 것

- 미결 항목을 혼자 정하고 진행하지 말 것.
- **수치를 확정된 것처럼 다루지 말 것.** `02-ECONOMY.md` 의 숫자는 전부
  시뮬레이션 검증 대기 중인 잠정값이다.
- Electron 을 아직 붙이지 말 것.
- Pixi 로 HUD/버튼을 만들지 말 것.
- 종류×등급 조합을 스프라이트로 미리 굽지 말 것.
- **오프라인 진행을 구현하지 말 것.** 이 게임에는 없다.
- **콤보/크리티컬을 넣지 말 것.** 검토했고 v1 제외다.
- **연타로 부수는 경로를 넣지 말 것.** 왁뿌볼은 **꾹 누르기 하나**로만 부순다
  (2026-09 확정). 클릭 반복은 타격음이 균열음을 덮고 손목을 아프게 한다.
  키캡 클릭은 별개이고 그대로 유지된다.
- 이름을 임의로 짓지 말 것. 전부 `docs/08-NAMING.md` 의 placeholder 를 쓴다.
- 왁뿌볼 50종을 손으로 나열하지 말 것. 희귀도 테이블 + 공식으로 생성한다.
- 에셋을 개별 PNG 로 로드하지 말 것. AssetPack 아틀라스를 쓴다.
