# 클로드 코드 프롬프트 모음

복붙용. **2인 작업이므로 자기 레인 것만 쓴다.**
분담 전체는 `docs/11-WORKSTREAMS.md`.

| | 레인 A · 엔진 | 레인 B · 감각 | 공동 |
|---|---|---|---|
| 순서 | 3 → 4 → 5 → 6 → 7 | 9 → 8 → 11 → 12 | 0, 13 |
| 영역 | `src/core/` `scripts/` | `render/` `audio/` `ui/` `assets/` | — |

**1·2번은 끝났다. 다시 돌리지 말 것** — 설정과 i18n 이 날아간다.

10번(룰렛)은 둘로 갈린다. `roll()` 순수 함수는 A 의 6번에 이미 들어 있고,
슬롯 연출과 확률 표시 화면이 B 의 몫이다.

---

## 0. 세션 시작 (매번)

```
CLAUDE.md 와 docs/11-WORKSTREAMS.md 를 먼저 읽어.
나는 레인 __ 다.   ← A 또는 B 를 적을 것

내 레인의 담당 문서와 docs/01-OPEN-QUESTIONS.md 를 읽고 네 가지만 말해줘:
1. docs/11-WORKSTREAMS.md 의 "다음 작업" 표에서 지금 내가 할 항목
2. 그 작업에 걸리는 미결 항목이 있는가
3. 코드와 문서가 어긋나는 부분
4. 지금 가장 위험하다고 보는 것 하나

규칙:
- 미결 항목에 의존하는 작업이면 진행하지 말고 먼저 물어봐
- 내 레인이 아닌 파일은 고치지 마. 필요하면 뭘 요청해야 하는지 말해줘
- 계약(src/core/state.ts)을 건드려야 하면 먼저 알려줘. 상대 레인이 막힌다
```

**레인을 안 적으면 에이전트가 남의 파일을 고친다.** 매번 적을 것.

---

## 1. 환경 세팅  ✅ 완료 — 다시 돌리지 말 것

```
docs/09-DEV-ENV.md 를 읽고 개발 환경을 맞춰줘.

- 부족한 패키지 설치 (howler, @assetpack/core, tsx, @types/howler)
- package.json scripts 를 문서의 표대로
- 폴더 구조 생성 (i18n/, audio/, scripts/, assets/, tests/fixtures/)
- assetpack 설정 파일
- 해상도를 640×360 으로 (src/render/app.ts 의 VIEW_W/VIEW_H, 현재 480×270)
  키캡 72×72. 깨지는 좌표 계산을 전부 새 기준으로 다시

끝나면 npm run check 와 npm run dev 가 도는지 확인하고 보고해줘.
```

---

## 2. i18n  ✅ 완료 — 다시 돌리지 말 것

```
docs/10-I18N.md 를 읽고 src/i18n/ 을 만들어줘.

- JSON 로드 + t(key) 만 하는 최소 구현. i18next 같은 라이브러리는 쓰지 마
- locales.ts 에 LOCALES 배열. { code, steamCode, label, font, dir }
  T0/T1 9개 언어를 전부 등록. JSON 은 ko/en 만 채우고 나머지는 빈 파일로
- docs/08-NAMING.md 의 placeholder 키를 전부 등록.
  값은 "[ball.type.001]" 처럼 키를 그대로 넣어둬 (미정 상태를 눈에 보이게)
- game.title.ko = "나만 없어 왁뿌볼", game.title.en = "Clack & Crackle"
  ★ 제목은 이 둘만 확정이다. 다른 언어는 전부 en 을 쓴다
- 폴백: 현재 로케일 → en → "[key]". 빈 문자열로 만들지 마
- 누락 키는 콘솔에 모아서 한 번에 찍어. 그게 번역 체크리스트가 된다
- tests/i18n.test.ts — 키 집합 일치 검사. T0/T1 은 실패, T2 이하는 경고
- 이미 하드코딩된 한국어 문자열이 있으면 전부 여기로 옮겨

★ 절대 조건: 언어 추가가 JSON 1개 + locales.ts 1줄로 끝나야 한다.
설정 화면의 언어 목록도 LOCALES 를 읽어서 그려. 언어를 추가할 때
UI 코드를 고치게 만들면 다시 짜야 한다.

★ 콘텐츠 데이터보다 먼저 하는 이유: 고유명사가 74개이고 9개 언어로 나간다.
나중에 하면 전부 뜯어내야 한다.
```

---

## 3. 상태 구조 (2차 환생 여지)  &nbsp;·&nbsp; **A · 엔진**

```
docs/02-ECONOMY.md 의 "2차 환생 여지" 대로 GameState 를 3층으로 나눠줘.

- 1차 리셋: 골드, 필드, 장착, 룰렛 카운터
- 2차 리셋 대상 (지금은 유지): 환생재화, 스킬트리, 키캡, 종류 해금/레벨, 도감
- 영구: 누적 통계, 업적, 설정

save.ts 도 이 구조로. 2차 환생은 구현하지 마. 자리만 만드는 거야.

★ GameEvent 도 같이 정의해줘. 이게 레인 B 와의 계약이라 여기서 빠진 이벤트는
  B 가 영원히 소리를 못 낸다. docs/04-AUDIO.md 의 사운드 17종을 전부 대조해:

  cap_press / ball_crack_1~5 / ball_shatter / ball_spawn / coin
  sting_rare·epic·legendary·supreme
  roulette_roll / roulette_result / roulette_fail
  type_unlock / type_levelup / frag_get / cap_complete
  dex_new / rebirth / skill_buy / ui_denied / field_full

  균열 이벤트에는 단계(1~5)가 실려야 한다. 진행률 20% 간격이고
  클릭 수가 아니라 진행률로 계산한다 (docs/02-ECONOMY.md "균열 5단계").
  꾹 누르기는 같은 5단계를 연속으로 지나간다.

  ★ 끊어 누르기 때문에 "꾹 시작 / 꾹 중단" 도 이벤트여야 한다.
    오디오가 연속 균열음을 시작하고 페이드아웃할 지점이라서다.
    떼도 진행률은 유지된다 — 상태에 남고 이벤트로만 알린다.
  ★ 파괴 이벤트에 방식('tap'|'hold'|'auto')이 실려야 한다.
    자동 파괴도 소리가 나야 하는데 볼륨이 다르다.

끝나면 B 에게 알려줘. 계약이 서기 전엔 B 가 시작을 못 한다.
```

---

## 4. 세이브 마이그레이션 하네스  &nbsp;·&nbsp; **A · 엔진**

```
docs/09-DEV-ENV.md 의 "세이브 마이그레이션 테스트" 대로 만들어줘.

- tests/fixtures/ 에 현재 버전 세이브를 파일로 저장
- tests/save-migration.test.ts 가 픽스처를 읽어 현재 코드로 로드되는지 검증
- save.ts 에 버전 번호와 마이그레이션 함수 자리를 만들어

★ 인크리멘탈에서 세이브 손실은 치명적이다. 수십 시간이 증발한다.
지금 만들어두면 출시 후 패치가 안전해진다.
```

---

## 5. 콘텐츠 데이터  &nbsp;·&nbsp; **A · 엔진**

```
docs/02-ECONOMY.md 의 표를 src/core/content/ 로 옮겨줘.

- balls.ts     종류 50 + 등급 5. rarity(★) 와 grade(등급)를 다른 필드로 분리
- keycaps.ts   24종 (C8/R6/E5/L3/S2)
- roulette.ts  확률표, 비용 곡선
- skilltree.ts 노드 8개 + 자동화 3단계

★ audio.ts 는 만들지 마. 같은 폴더에 있지만 레인 B 소유다
  (docs/11-WORKSTREAMS.md 파일 소유권)

규칙:
- 전부 데이터. 로직 금지
- ★ 50종을 손으로 나열하지 마. 희귀도 테이블 + 공식으로 생성할 것
  (docs/09-DEV-ENV.md 의 RARITY_BANDS 예시 참조)
- 이름은 nameKey 로만. 한국어 문자열 금지
- 같은 등급 안에서 배율에 ±20% 폭

타입만 잡고 끝내지 말고 실제 값을 다 채워.
```

---

## 6. 엔진  &nbsp;·&nbsp; **A · 엔진**  🟡 Q11(연타+꾹 배율) 필요

```
docs/02-ECONOMY.md 대로 core 엔진을 짜줘.

순수 함수:
- pressKeycap(state, rng)         골드 + N타마다 왁뿌볼 생산
- hitBall(state, idx, mode)       mode: 'tap' | 'hold' | 'auto'
- resolveFieldFull(state, choice) 교체 | 즉시파괴
- roll(state, rng)                룰렛. 종류 슬롯 처리 포함
- rebirth(state)                  환생재화 = √(누적 골드) × 보정
- buySkill(state, nodeId)

주의:
- 종류 슬롯: 해금은 희귀도 추첨, 레벨업은 균등 랜덤. 신규 우선 없음
- 도감은 종류 단위 50칸, 칸마다 본 등급 비트
- 자동 파괴는 **등급별 해금**이다. 스킬트리 노드 레벨(1~5)이 자동으로
  깨지는 등급의 상한이다. "처음 보는 조합" 규칙은 폐기됐다
- 진행률은 0~1 하나로 관리한다. 연타(이산) / 꾹(연속)이 같은 값을 올리고,
  **손을 떼도 리셋되지 않는다.** 파괴 1회가 약 3초다
- 오프라인 진행은 구현하지 마
- ★ 성능 이유로 mutable 하게 만들지 마. 20시간 시뮬이 91ms 로 측정됐다
- GameEvent 에 렌더/오디오가 구독할 이벤트를 전부

끝나면 npm test.
```

---

## 7. 밸런스 리포트 CLI  &nbsp;·&nbsp; **A · 엔진**

```
scripts/sim.ts 를 만들어줘. tsx 로 실행한다.

docs/02-ECONOMY.md 의 "감시할 지표" 4개를 출력:
1. 첫 환생까지 시간 (목표 10분)
2. 시간대별 골드 수입 출처 비율 (키캡 / 왁뿌볼 / 방치 / 환급)
3. 20시간 시점 골드 자릿수 (최소 1e12)
4. 50종 해금 완료 시점 (목표 8~10시간)

2번에서 후반 키캡 클릭 비중이 0 에 수렴하면 경고를 찍어.
화면 주인공이 무의미해진 거니까.

기준 미달 항목은 눈에 띄게 표시해. 다만 수치를 임의로 고치지는 마.
```

```
docs/02-ECONOMY.md 의 "목표 페이싱" 표를 tests/balance.test.ts 단언으로 옮겨줘.
지금 통과 못 하는 게 정상이야. 실패를 남겨두고 왜 실패하는지 정리해줘.
```

---

## 8. 왁뿌볼 렌더 (직교 구조)  &nbsp;·&nbsp; **B · 감각**  🟢 막힌 것 없음

```
왁뿌볼 렌더를 구현해줘. docs/03-ART.md 의 직교 구조를 지킬 것.

- 종류 스프라이트 (아직 없으니 Graphics 임시, 교체 가능하게 분리)
- 등급은 tint + filter 로 런타임 적용
- 균열은 공용 오버레이 5장을 위에 얹는 방식
- ★ 희귀도는 화면의 왁뿌볼에 표시하지 않는다. 도감에서만

조합을 스프라이트로 미리 굽지 마. 250개 텍스처를 만들면 설계 위반이야.

★ 캐주얼 아트라 종류 그림이 베이스컬러/셰이딩 두 레이어로 온다
  (docs/03-ART.md "틴트가 자연스럽게 먹으려면"). tint 는 베이스컬러
  레이어에만 곱하고 셰이딩 레이어는 그 위에 원본 그대로 얹어.
  아직 실제 그림이 없으니 Graphics 임시에서도 이 두 레이어 분리를
  흉내 낼 수 있게 구조를 잡아둬 — 나중에 그림이 들어올 때 바꿀 게 없게.
```

---

## 9. 오디오  &nbsp;·&nbsp; **B · 감각**  ← B 는 여기부터

```
docs/04-AUDIO.md 를 읽고 오디오 시스템을 구현해줘.

- howler.js. src/audio/ 가 GameEvent 를 구독. core 는 howler 를 모른다
- 필수 처리 7가지를 전부: 랜덤 변주, 피치 변주, 동시 재생 상한,
  꾹 누르기 전용 사운드, 자동 파괴 볼륨 차등, 볼륨 슬라이더, ogg 우선
- 음원 파일이 없으면 조용히 넘어가고 콘솔에 비어 있는 키 목록을 찍어.
  그게 녹음 체크리스트가 된다

★ 파괴 1회가 약 3초, 단계당 0.6초다. 균열음 하나가 그 안에 들어가야 한다.
★ 끊어 누르기 — 떼면 연속 균열음을 짧게 페이드아웃, 다시 누르면 멈춘
  단계에서 이어서 새로 소리를 낸다. 1단계로 되감지 마.
  끊어 누르는 리듬으로 소리를 만드는 게 이 게임의 정체성이다.
★ 자동 파괴도 소리가 난다. 조용하게 하되 끄지 마 — 자동이 켜진 후반이
  플레이 시간의 대부분이라 여기서 죽이면 게임이 무음이 된다.
```

---

## 10. 룰렛  &nbsp;·&nbsp; **연출·확률화면은 B / roll() 은 A 의 6번**

```
룰렛을 구현해줘. 단일 슬롯 버튼 하나.

- 확률표는 docs/02-ECONOMY.md
- 비용 100 × 1.04^(이번 판 횟수). 버튼에 항상 표시
- 종류 슬롯: 미보유 있으면 해금(희귀도 추첨), 없으면 레벨업(균등)
- 확률 표시 화면 별도로. 법적 의무는 아니지만 반드시 넣는다
- 종류 신규 해금 연출은 50번 볼 화면이야. 인상적이되 스킵 가능하게
```

---

## 11. 보관함 확대 화면  &nbsp;·&nbsp; **B · 감각**  🟡 방향 확정, 세부는 실플레이 후

```
필드가 꽉 찬 상태에서 새 왁뿌볼을 얻었을 때의 화면을 만들어줘.

- 새로 얻은 것 + 필드 5개를 확대해서 나란히
- 교체(필드 하나를 골라 부수고 그 자리에) 또는 즉시 파괴 선택
- 각각의 종류/등급/예상 골드가 한눈에

★ 자주 뜨는 화면이다. 클릭 수를 최소로 하고 마찰을 줄여.

화면이 뜨는 조건 (docs/02-ECONOMY.md §4):
- 새 왁뿌볼의 등급이 자동 파괴 해금 범위 안이면 → 화면 없이 자동 파괴
- "항상 이렇게" 설정이 있으면 → 저장된 선택을 그대로 적용
- 둘 다 아니면 → 이 화면

"항상 이렇게" 체크박스를 화면 안에 넣고, 설정에서 해제할 수 있게 해.
세부(클릭 수, 등급별로 기억할지)는 실제로 플레이해 보고 정한다 — 일단
가장 단순한 형태로 만들고 마찰을 재보자.
```

---

## 12. 스크린샷 리뷰 (반복)  &nbsp;·&nbsp; **B · 감각**

```
npm run shot 을 돌리고 shots/ 이미지를 봐.

각각에 대해:
- 에셋이 흐려진 곳은 없는가 (표시 크기의 2배 미만 원본을 확대한 건 아닌가)
- 안티에일리어싱이 제대로 켜져 있는가 (곡선이 매끄러운가)
- 왁뿌볼이 화면의 주인공으로 보이는가
- 키캡과 필드 사이 거리가 적절한가 (수천 번 왕복할 동선이다)
- 정보 위계가 맞는가

문제가 있으면 고치고 다시 찍어.
"괜찮아 보입니다"로 끝내지 말고 구체적으로 말해줘.
```

---

## 13. 규칙 위반 점검 (주기적으로)  &nbsp;·&nbsp; **공동 — 각자 자기 항목만**

```
CLAUDE.md 의 절대 규칙 10개를 하나씩 검사해줘.

- src/core/ 의 pixi / howler import
- engine.ts 의 숫자 리터럴
- src/core/content/ 밖의 밸런스 수치
- 표시 크기의 2배 미만으로 제작된 에셋
- 종류×등급 조합을 미리 구운 텍스처
- 종류 그림이 베이스컬러/셰이딩 레이어로 안 나뉜 곳
- 오프라인 진행 코드
- rarity 와 grade 를 혼용한 곳
- 하드코딩된 한국어 문자열 (i18n 키를 안 쓴 곳)
- core 가 mutable 로 바뀐 곳
```

---

## 14. Electron / 스팀 (게임이 재밌어진 뒤)  &nbsp;·&nbsp; **나중**

```
docs/05-SHIP.md 의 "빌드" 체크리스트 7개를 전부 지켜서 Electron 을 붙여줘.
타겟 dir(nsis 아님), asarUnpack, 버전 고정, steam_appid.txt 제외.

src/platform/steam.ts 가 인터페이스를 잡아뒀으니 게임 코드는 건드리지 말고
preload/main 만 채워.
```

---

## 쓰지 말 것

```
❌ "밸런스 좀 잘 맞춰줘"   → 목표 페이싱 표를 근거로
❌ "재밌게 만들어줘"       → 판단을 위임하면 평균이 나온다
❌ "알아서 해줘"           → 미결 항목을 임의로 정하고 진행한다
❌ "전부 다 만들어줘"      → 검증 없이 쌓인 코드는 다 다시 봐야 한다
❌ "성능 최적화 해줘"      → core 를 mutable 로 바꾼다. 91ms 면 충분하다
```

2인 작업이라 하나 더:

```
❌ "이것도 김에 같이 고쳐줘"  → 남의 레인 파일을 건드린다.
                              "잠깐이면 되는데"가 이 프로젝트에서 가장 비싼 문장이다
```
