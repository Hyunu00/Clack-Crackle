# 10 — 현지화 (i18n)

이 문서는 **언어를 나중에 추가할 수 있게 만드는 것**이 목적이다.
출시 시점의 언어 목록보다 "추가 절차가 싸다"는 성질이 중요하다.

`docs/09-DEV-ENV.md` 의 "반드시 지켜야 하는 것 ①" 을 구현한 문서다.

---

## 게임 제목 (🟢 확정)

| 언어 | 제목 |
|---|---|
| 한국어 | **나만 없어 왁뿌볼** |
| **그 외 전 언어** | **Clack & Crackle** |

`Clack` = 키캡 타건음, `Crackle` = 왁스 파열음. 두 소재를 하나씩 맡는다.
둘 다 의성어라 유행어가 없다 (`docs/06-DECISIONS.md` 의 Q2 결정과 일치).

한국어만 현지화 제목으로 유행어를 태운다. **영문 주제목이 스토어 URL 과
앱 식별자를 결정하므로 현지화 제목의 변경 비용은 훨씬 낮다.**
"되돌리기 비용이 낮은 자리에 유행을 태운다"는 Q2 원칙 그대로다.

밈 형식이라 다른 언어로 옮길 수 없다. **한국어 외에는 번역하지 말고
`Clack & Crackle` 을 그대로 쓴다.**

주의: 한국어 제목이 9자다. 타이틀 화면 로고를 640×360 안에 앉힐 때
영문보다 폭이 넓다. `docs/03-ART.md` 의 로고 작업 시 두 벌을 상정할 것.

---

## 지원 언어

스팀 언어 점유율 (2026-03 하드웨어 설문):

| 순위 | 언어 | 점유율 |
|---|---|---|
| 1 | 영어 | 39.09% |
| 2 | 중국어 간체 | 22.75% |
| 3 | **러시아어** | 9.21% |
| 4 | 스페인어 | 4.81% |
| 5 | 포르투갈어(브라질) | 4.42% |
| 6 | 독일어 | 2.96% |
| 7 | 프랑스어 | 2.46% |
| 8 | 일본어 | 2.24% |
| 9 | 폴란드어 | 1.82% |
| 10 | 한국어 | 1.69% |

상위 5개가 전체의 약 80%다. 일본어·한국어는 점유율은 낮지만
**플레이어당 지출이 평균 이상**이라 ROI 가 따로 선다.

### 티어

| | 언어 | 스팀 코드 | BCP-47 | 폰트 프로필 |
|---|---|---|---|---|
| **T0** | 한국어 | `koreana` | `ko` | `ko` |
| **T0** | 영어 | `english` | `en` | `latin` |
| **T1** | 중국어 간체 | `schinese` | `zh-CN` | `zh` |
| **T1** | 러시아어 | `russian` | `ru` | `cyrillic` |
| **T1** | 일본어 | `japanese` | `ja` | `ja` |
| **T1** | 스페인어 | `spanish` | `es` | `latin` |
| **T1** | 포르투갈어(브라질) | `brazilian` | `pt-BR` | `latin` |
| **T1** | 독일어 | `german` | `de` | `latin` |
| **T1** | 프랑스어 | `french` | `fr` | `latin` |
| **T2** | 중국어 번체 | `tchinese` | `zh-TW` | `zh` |
| **T2** | 폴란드어 | `polish` | `pl` | `latin` |
| **T2** | 터키어 | `turkish` | `tr` | `latin` |
| **T2** | 우크라이나어 | `ukrainian` | `uk` | `cyrillic` |
| **T2** | 스페인어(중남미) | `latam` | `es-419` | `latin` |
| **T2** | 이탈리아어 | `italian` | `it` | `latin` |
| **T3** | 아랍어 | `arabic` | `ar` | `fallback` + RTL |
| **T3** | 태국어 | `thai` | `th` | `fallback` |
| **T3** | 베트남어 | `vietnamese` | `vi` | `latin`(확장) |
| **T3** | 인도네시아어 | `indonesian` | `id` | `latin` |

- **T0** — 1일차부터. 개발 언어
- **T1** — 출시 시점 목표. 9개
- **T2** — 출시 후 패치. 반응 보고 추가
- **T3** — 조건부. 아래 참조

**T0 과 T1 만 `npm run check` 에서 키 누락이 에러다.** T2 이하는 경고.

### 목록에서 뺀 것

**힌디어 / "인도어"** — 스팀의 전체 플랫폼 지원 언어 목록에 힌디어가 없다.
인도 스팀 사용자는 영어로 플레이한다. 넣을 자리가 없다.

**아랍어는 T3 로 내렸다.** 요청에 있었지만 비용이 다른 언어와 급이 다르다:

1. **RTL** — UI 레이아웃 전체가 좌우 반전된다. HUD 가 HTML 이라
   `dir="rtl"` 로 상당 부분 해결되지만 (Pixi 로 HUD 를 안 만든 결정이
   여기서 이득을 본다), Pixi 캔버스 안의 텍스트는 별도 처리다.
2. **글자 결합(shaping)** — 아랍어는 글자가 앞뒤 문맥에 따라 네 가지
   형태로 바뀌고 서로 이어진다. **비트맵 픽셀 폰트로는 사실상 불가능하다.**
   시스템 폰트로 폴백해야 하고, 그러면 **그 언어에서만 픽셀아트 톤이 깨진다.**

넣지 말자는 게 아니라, **T1 을 다 끝내고 별도 작업으로 잡으라**는 뜻이다.
점유율 상위 10위 안에도 없다.

---

## 언어를 추가하는 절차

**이 절차가 7단계를 넘어가면 설계가 잘못된 것이다.**

```
1. 이 문서의 티어 표에 한 줄 추가 (스팀 코드 + BCP-47 + 폰트 프로필)
2. src/i18n/{BCP-47}.json 생성 — en.json 을 복사해서 시작
3. src/i18n/locales.ts 의 LOCALES 배열에 한 줄 추가
      { code, steamCode, label, font, dir }
4. 폰트 프로필 확인 — 그 문자를 픽셀 폰트가 커버하는가
      커버 못 하면 font 를 'fallback' 으로
5. npm test — 누락 키 테스트가 빠진 키를 전부 나열한다
6. 번역. 미번역 키는 자동으로 영어로 표시되므로 부분 번역 상태로 커밋해도 된다
7. 스팀: 스토어 페이지 현지화 + "지원 언어" 표에 체크
```

**설정 화면의 언어 목록은 `LOCALES` 를 읽어서 그린다.**
언어를 추가할 때 UI 코드를 고치는 일이 있으면 안 된다.

---

## 구조

```
src/i18n/
  index.ts      t(key, params?) + 로케일 전환
  locales.ts    LOCALES 배열. 언어 추가 시 유일하게 고치는 코드 파일
  ko.json
  en.json
  zh-CN.json
  ...
```

게임용이라 i18next 는 과하다. JSON 로드 + `t(key)` 로 충분하다.

### 폴백 규칙

```
t(key)
  1. 현재 로케일에 있으면        → 그 값
  2. 없으면 en.json              → 영어
  3. en 에도 없으면              → "[key]" 를 그대로 출력
```

3번을 빈 문자열로 만들지 말 것. **미번역이 화면에서 눈에 띄어야 한다.**
누락 키는 콘솔에 모아서 한 번에 찍는다 — 그게 번역 체크리스트가 된다.
(`docs/04-AUDIO.md` 의 빈 사운드 키 처리와 같은 방식이다.)

### 키 규칙

```
ball.type.001 ~ 050      왁뿌볼 종류 50
cap.001 ~ 024            키캡 24
sys.*                    시스템 용어
skill.* / auto.*         스킬트리 11
ach.<ID>.name / .desc    업적 26
ui.*                     버튼, 라벨, 설정
game.title.*             제목
```

콘텐츠 데이터는 **`nameKey` 만 갖는다.** 문자열을 직접 갖지 않는다.

```ts
{ id: 1, nameKey: "ball.type.001", rarity: 1, baseValue: 40 }   // O
{ id: 1, name: "사과", rarity: 1, baseValue: 40 }                // X
```

---

## 폰트 매트릭스

**픽셀 폰트의 상업 이용 라이선스와 글리프 커버리지를 반드시 확인할 것.**
`docs/03-ART.md` 의 에셋 목록에도 같은 항목이 있다.

| 프로필 | 대상 | 요구 글리프 |
|---|---|---|
| `latin` | en de fr es pt-BR pl tr it vi id | 라틴 + **확장 라틴** |
| `cyrillic` | ru uk | 키릴 |
| `ko` | ko | 한글 |
| `ja` | ja | 가나 + 상용한자 |
| `zh` | zh-CN zh-TW | 중문 상용자 |
| `fallback` | ar th | 시스템 폰트. 픽셀 톤 포기 |

**확장 라틴을 빠뜨리지 말 것.** 자주 사고가 나는 지점이다.

```
독일어   ä ö ü ß
프랑스어  é è ê ç à ù
스페인어  ñ á í ó ú ¿ ¡
폴란드어  ł ą ę ś ż ź ć ń
터키어   ı İ ş ğ ç ö ü      ← 점 없는 ı 와 점 있는 İ 가 별개 글자다
포르투갈어 ã õ ç á â
베트남어  성조 결합 기호 다수 — 커버리지가 가장 넓다
```

**CJK 픽셀 폰트는 선택지가 극히 적다.** 한/중/일이 한 폰트로 안 되면
프로필별로 다른 폰트를 물리는 구조를 미리 잡을 것. 나중에 붙이면 렌더러를
뜯어야 한다.

---

## 번역 분량

| 대상 | 개수 | 난이도 |
|---|---|---|
| UI · 시스템 문자열 | 약 150~250 | 낮음 |
| **왁뿌볼 종류** | **50** | **높음 — 고유명사** |
| **키캡** | **24** | **높음 — 고유명사** |
| 스킬트리 노드 이름 + 설명 | 11 × 2 | 중간 |
| 업적 이름 + 설명 | 26 × 2 | 중간 |
| 등급 · 희귀도 표기 | 10 | 낮음 |
| 스토어 페이지 | 1 세트 | 중간 |

총 400~500 키. 클리커라 분량 자체는 작다.

**비용의 대부분은 고유명사 74개다.** 이름에는 말장난과 톤이 들어가서
기계번역이 특히 나쁘다. `docs/08-NAMING.md` 에서 한국어 이름을 확정할 때
**번역 가능성을 같이 볼 것.** 옮길 수 없는 말장난은 그 언어에서 다른
말장난으로 바꿔야 하고, 그건 번역이 아니라 재창작이라 비싸다.

**스토어 페이지 번역이 게임 내 번역보다 ROI 가 높다.** 노출에 직접 걸린다.
T2 언어는 스토어만 먼저 번역하는 선택지도 있다.

---

## 검증

`tests/i18n.test.ts`:

1. **키 집합 일치** — 모든 로케일이 `en.json` 의 키를 100% 갖는가.
   T0/T1 은 실패, T2 이하는 경고
2. **빈 값 없음** — 값이 빈 문자열인 키가 없는가
3. **placeholder 잔존** — `"[ball.type.001]"` 형태가 T0/T1 에 남아 있지 않은가
4. **파라미터 일치** — `{n}` 같은 치환 토큰이 원문과 번역문에서 같은가

`npm run check` 에 포함된다.

### 화면 검증

`npm run shot` 을 **언어별로** 돌릴 것. 텍스트 길이가 언어마다 다르다.

```
독일어    영어 대비 약 1.3배. UI 가 가장 잘 터진다
러시아어  영어 대비 약 1.2배
CJK      짧지만 글자가 크다
```

**독일어에서 안 터지면 대체로 다 통과한다.** 독일어를 기준으로 볼 것.

---

## 스팀 스토어

- [ ] 지원 언어 표 — 인터페이스 / 음성 / 자막을 각각 체크.
      **이 게임은 음성이 없으므로 인터페이스만 체크한다**
- [ ] 스토어 페이지 현지화 (T1 전부)
- [ ] **업적 이름·설명 현지화** — 업적은 스팀 쪽에 따로 등록한다.
      게임 안 JSON 과 별개다. 빠뜨리기 쉽다
- [ ] 한국어 스토어에만 현지화 제목 `나만 없어 왁뿌볼` 지정
- [ ] 캡슐 이미지의 제목 텍스트 — 언어별로 만들지 여부 결정.
      **한국어 캡슐은 별도로 만들어야 한다.** 제목이 다르다
