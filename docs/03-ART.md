# 03 — 아트

## 해상도 규격 (확정)

| 항목 | 값 |
|---|---|
| 논리 해상도 | **640 × 360** |
| 화면 배율 | ×3 → 1920×1080 정확히 일치 |
| 키캡 | **72 × 72** (화면상 216px = 세로 1/5) |
| 왁뿌볼 | 40 × 40 |
| 필드 슬롯 | 48 × 48 |
| 도감 썸네일 | 24 × 24 |
| 필터 | nearest. linear 절대 금지 |
| 좌표 | 정수만 |

**1024×1024 는 픽셀아트 규격이 아니다.** 그 크기로 그려서 216px 로 축소하면
픽셀이 뭉개져 픽셀아트가 아니게 된다. 72×72 로 직접 그릴 것.

🟢 코드 반영 완료 — `src/render/app.ts` 의 `VIEW_W`/`VIEW_H` 가 640/360 이고
`tests/env.test.ts` 가 이 값과 정수 배율을 단언한다.

---

## 왁뿌볼 — 250개를 그리지 않는 법

종류 50 × 등급 5 = 250조합인데, 실제 제작물은 **60장**이다.

| 요소 | 담당 | 제작물 |
|---|---|---|
| 종류 | 모양 | 스프라이트 **50** |
| 등급 | 색 + 이펙트 | 틴트/필터 **5** |
| 균열 | 파괴 단계 | **공용 오버레이 5** |

**균열을 종류별로 그리지 말 것.** 왁스 껍질이 깨지는 표현이라
반투명 오버레이 5장을 종류 위에 얹으면 된다. 소재상으로도 자연스럽다.
종류별로 그리면 250프레임이 된다.

**5장은 진행률 20 / 40 / 60 / 80 / 100% 에 대응한다** (🟢 2026-08 확정).
5번이 파괴 직전의 최대 균열 상태다. 단계 정의는 `docs/02-ECONOMY.md` 의
"균열 5단계" 가 원본이고, 사운드도 같은 5단계다 (`docs/04-AUDIO.md`).
**꾹 누르면 이 5장이 연속으로 갈아끼워진다.** 한 장 한 장이 아니라
이어지는 한 동작으로 보여야 한다.

| 등급 | 표현 |
|---|---|
| Common | 원본 색 |
| Rare | 초록 틴트 + 얇은 테두리 |
| Epic | 보라 틴트 + 발광 테두리 |
| Legendary | 금 틴트 + 발광 + 반짝 파티클 |
| Supreme | 프리즘(색상 회전) + 상시 파티클 |

Supreme 표현 방식은 미결 (`01-OPEN-QUESTIONS.md` Q4).

**★ 희귀도는 왁뿌볼 자체에 표시하지 않는다.** 도감에서만 별로 보여준다.
화면의 왁뿌볼은 등급 색만 입는다. 두 축이 화면에서 겹치면 혼란스럽다.

---

## 팔레트

`src/render/palette.ts` 와 `index.html` CSS 변수 두 곳. **항상 같이 고칠 것.**

```
--case   #14161c   케이스 (배경)
--plate  #272d3a   플레이트
--cap    #e8e2d4   키캡 상단 (PBT 베이지)
--amber  #f2b705   골드 / 강조
--jade   #6ec3c1   보조
```

배경을 어둡게 유지하는 이유: 왁뿌볼이 부서질 때 파편과 빛이 튀어야 하는데
밝은 배경에서는 안 보인다. 흰색 Common 도 어두운 배경이라 충분히 보인다.

**왁뿌볼 50종은 이 팔레트에 매이지 않는다.** 알록달록해야 소재에 맞다.
다만 배경/UI 와 충돌하지 않게 채도 상한은 둘 것.

---

## 에셋 목록

### 필수

- [ ] 키캡 **24종**, 72×72 (눌림은 코드에서 y 오프셋 처리)
- [ ] 왁뿌볼 **50종**, 40×40
- [ ] 균열 오버레이 **5단계** (공용), 40×40 반투명
- [ ] 파편 4~6종
- [ ] 필드 슬롯 프레임 (빈칸 / 채워짐 / 선택됨)
- [ ] 골드 / 환생재화 / 조각 아이콘 (조각은 등급별 5색)
- [ ] 별 아이콘 (도감 희귀도용, 채움/빈칸)
- [ ] 등급 점 아이콘 (도감용, 5색 + 빈칸)
- [ ] UI 9-slice 패널, 버튼 4상태
- [ ] 픽셀 폰트 — **상업 이용 라이선스 확인 필수.** 한글 지원 여부도.
      CJK 픽셀 폰트는 선택지가 적으니 폴백 구조를 미리 잡을 것

### 연출

- [ ] 균열 파티클
- [ ] 등급별 획득 이펙트 5종
- [ ] Supreme 전용 연출 (화면 전체 반응)
- [ ] **종류 신규 해금 연출** — 50번 볼 화면이다. 인상적이되 스킵 가능하게
- [ ] 종류 레벨업 연출 (해금보다 가볍게)
- [ ] 룰렛 슬롯 회전
- [ ] 보관함 확대 화면
- [ ] 화면 흔들림 (코드)

### 스팀 스토어

- [ ] 헤더 캡슐 460×215 / 소형 231×87 / 메인 616×353
- [ ] 세로형 374×448 / 라이브러리 600×900 / 히어로 3840×1240
- [ ] 라이브러리 로고 (투명 PNG)
- [ ] 스크린샷 5장 이상 (1920×1080)
- [ ] 트레일러 — **첫 5초 안에 왁뿌볼이 부서져야 한다**
- [ ] 업적 아이콘 (잠김/해제 × 26)

**캡슐에 텍스트를 많이 넣지 말 것.** 축소되면 안 읽힌다.

---

## 에셋 파이프라인 (AssetPack)

에셋이 90장을 넘는다. 개별 PNG 로 로드하면 요청 90번에 Pixi 배칭도 못 쓴다.

```
assets/               ← raw PNG 를 여기에
  balls{tps}/
  keycaps{tps}/
  cracks{tps}/
  ui{tps}/
  fx{tps}/
        │  npm run assets
        ▼
public/atlas/         ← 폴더당 스프라이트시트 1장 + .png.json
```

**폴더 이름의 `{tps}` 는 오타가 아니다.** AssetPack 은 이 태그가 붙은 폴더만
스프라이트시트로 묶는다. 태그를 빼면 PNG 가 그냥 복사만 되고 아틀라스가
안 나온다. 새 에셋 폴더를 만들 때도 `{tps}` 를 붙일 것.

런타임 코드는 아틀라스에서 **프레임 이름**으로 꺼내 쓴다.
`core/content/` 의 `spriteKey` 가 그 프레임 이름과 일치해야 한다.

**프레임 이름은 확장자를 포함한 파일명 그대로다** (`nameStyle: "short"`).

```
assets/balls{tps}/ball_001.png   →   프레임 이름 "ball_001.png"
```

→ `spriteKey: "ball_001.png"` 로 쓴다. 확장자를 빼면 못 찾는다.
`{tps}` 폴더끼리 같은 파일명이 있으면 AssetPack 이 충돌 경고를 낸다.
`docs/03-ART.md` 의 파일명 규칙(`ball_NNN.png` 등)을 지키면 충돌하지 않는다.

PNG 를 추가하거나 고친 뒤에는 반드시 `npm run assets` 를 다시 돌릴 것.

## 작업 규칙

- 안티에일리어싱 금지
- 논리 해상도 기준으로 직접 그린다. 크게 그려서 축소 금지
- 파일명: `cap_NNN.png`, `ball_NNN.png`, `crack_N.png`, `fx_*.png`, `ui_*.png`
  `core/content/` 의 키와 정확히 일치할 것
- **왁뿌볼 50종은 실루엣이 서로 구분되어야 한다.** 40×40 에서 색만 다르면
  도감에서 구별이 안 된다

도구: Aseprite / LibreSprite / Piskel
